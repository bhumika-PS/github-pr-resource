language: go
sudo: false
dist: focal
services:
  - docker
matrix:
  include:
  - os: osx
    go: 1.14.x
  - os: linux
    go: 1.14.x
notifications:
  email: false

script:
  - curl -sL https://taskfile.dev/install.sh | sh
  - ./bin/task ci
